{
  "$id": "https://www.austinpowder.com/specs/schemas/cost/v1.0",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Cost",
  "description": "A monetary cost",
  "type": "object",
  "properties": {
    "amount": {
      "type": "number",
      "description": "The monetary amount"
    },
    "currency": {
      "type": "string",
      "description": "The currency code this cost is denominated in. Eg: USD, AUD, EUR etc..."
    },
    "extensionData": {
      "type": "array",
      "description": "An arbitrary array of data not formally defined by the spec"
    },
    "notes": {
      "type": "string",
      "description": "Any notes that go along with this cost, such as whether the cost is per-unit etc..."
    }
  },
  "required": [ "amount" ]
}